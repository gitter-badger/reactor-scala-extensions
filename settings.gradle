rootProject.name = 'reactor-scala-extensions-root'

include 'reactor-scala'
include 'micrometer-test'

buildCache {
    local {
        enabled = true
    }
/*
    remote(HttpBuildCache) {
        url = "https://cache1.winar.to:32500/"
        enabled = true
        push = true
    }
*/
}
